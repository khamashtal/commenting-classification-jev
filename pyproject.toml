[project]
name = "jev-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.14.3",
    "mcp>=2.2.0",
    "python-dotenv>=1.2.3",
    "typesafe-sdk>=0.6.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.8",
]
